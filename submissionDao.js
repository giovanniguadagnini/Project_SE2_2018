const utilities = require('./utilities');
const connection = utilities.connection;


function getAllSubmissions(id_user){
    return [{}];
}

function getSubmissionById(loggedUser, idSubmission{
    return new Promise( resolve => {

    });
}

function updateSubmission(submission){
    return submission;
}

module.exports = {getAllSubmissions, getSubmissionById, updateSubmission};